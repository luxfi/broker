name: Docker Multi-Arch

on:
  push:
    branches: [main, dev]

permissions:
  contents: read
  packages: write

jobs:
  docker:
    uses: liquidityio/.github/.github/workflows/docker-build.yml@main
    with:
      image: ghcr.io/liquidityio/bd
      platforms: linux/amd64,linux/arm64
    secrets: inherit
